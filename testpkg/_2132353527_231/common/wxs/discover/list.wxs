function mapList(ids, entities) {
    if (ids && ids.length > 0 && entities) {
        return (ids.map((function(id) {
            return (entities[((nt_0 = (id.toString()), null == nt_0 ? undefined : 'number' === typeof nt_0 ? nt_0 : "" + nt_0))] || id)
        })))
    };
    return ([])
};
module.exports = ({
    mapList: mapList,
});